{
  "name": "template-fe",
  "private": true,
  "type": "module",
  "description": "Frontend paradigm templates \u2014 CSR / SSR / SSG. Not a workspace: each templates/* is a standalone project.",
  "scripts": {
    "sync": "bun scripts/sync.ts",
    "check": "bun scripts/sync.ts --check",
    "format": "cd templates/csr && bunx --bun prettier --write --config .prettierrc --ignore-path .prettierignore ../../shared ../../scripts ../../README.md"
  }
}
