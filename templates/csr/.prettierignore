bun.lock
dist
storybook-static
playwright-report
test-results
coverage

# 생성물 — 생성기의 포맷을 그대로 둔다
src/api
src/@types
**/*.gen.ts
public/mockServiceWorker.js
# Next.js가 `next dev`/`next build`마다 다시 쓴다(템플릿에 없으면 매칭 안 될 뿐이다)
next-env.d.ts
